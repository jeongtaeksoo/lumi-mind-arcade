# LUMI’S MIND ARCADE — Game Spec v2

## 한 줄 정의

루미가 플레이어의 최근 실수를 기억하고 다음 문제의 모드와 난이도를 조율하는 6모드, 5라운드 브라우저 아케이드입니다.

## 핵심 루프

`게임 시작 → 3개 스테이지 라운드 → 성공/실패 기록 → 다음 라운드 조율 → 결과와 실제 정확도 저장`

- 시작 하트는 5개입니다.
- 스테이지 성공 시 점수와 콤보가 오르고, 오답이나 시간 초과 시 하트가 1개 줄며 콤보가 0이 됩니다.
- 라운드 1~2의 6개 스테이지에는 여섯 모드가 중복 없이 한 번씩 등장합니다.
- 라운드 3~4는 최근 실패 비율이 높은 모드를 우선하되 한 라운드 안에서는 모드를 중복하지 않습니다.
- 라운드 5는 `최종 1`, `최종 2`처럼 3개 스테이지 사이클을 반복합니다.
- 하트가 0이 되면 즉시 결과를 확정하고, 진행 중 홈으로 나가면 완료 기록을 만들지 않습니다.
- 스테이지가 바뀔 때 모드명 전환 화면이나 입력 대기 배너를 띄우지 않고 다음 보드를 즉시 렌더링합니다. 모드명은 HUD 안에서만 갱신합니다.

## 여섯 미니게임

### 별빛 포착

움직이는 목표를 제한시간 안에 연속으로 찾습니다. 레벨이 오르면 필요한 적중 수와 방해 도형 수가 늘고 목표가 작아집니다.

### 빛 순서

카드가 빛난 공간 순서를 그대로 재현합니다. 초반에는 같은 카드가 한 시퀀스에서 중복되지 않으며, 높은 레벨부터 직전 카드만 연속되지 않는 중복 순서가 나올 수 있습니다.

### 균열 탐색

보드에서 규칙이 다른 타일 하나를 찾습니다. 모양 차이로 시작해 회전과 명도 차이가 추가되고 보드가 4×4에서 7×7까지 커집니다.

### 궤도 입력

표시된 방향을 순서대로 입력합니다. 화면 패드와 키보드 방향키를 모두 지원하며 길이는 4개에서 8개까지 증가합니다.

### 신호 전환

기준 신호와 `색 맞추기` 또는 `모양 맞추기` 규칙을 읽고 맞는 후보를 고릅니다. 한 스테이지 안에서도 규칙이 바뀌며, 낮은 난이도에서는 같은 규칙이 두 번 유지된 뒤 바뀌어 전환을 학습할 시간을 줍니다.

### 별길 추적

4×4 또는 5×5 격자에 잠깐 표시된 상하좌우 인접 경로를 시작 칸부터 다시 입력합니다. 터치와 클릭을 지원하고, 키보드에서는 방향키로 초점을 옮긴 뒤 Enter로 선택할 수 있습니다.

## 적응 규칙

- 최근 12개 시도의 `success`, `error`, `timeout`을 모드별로 보관합니다.
- 선택 가중치는 최근 실패 비율이 높을수록 커집니다. 라운드의 첫 모드는 가장 약한 모드이며 나머지 둘은 약점 가중 무작위 선택입니다.
- 라운드 3 이후 최근 시도가 4개 이상이고 정확도가 60% 미만이면 기본 난이도에서 1을 낮춥니다.
- 최근 시도가 8개 이상이고 정확도가 85% 이상이면 기본 난이도에서 1을 높입니다.
- 난이도는 항상 1~7 범위를 벗어나지 않습니다.

## 점수와 기록

- 성공 점수는 `모드 기본점수 + 난이도×10 + 남은 시간 보너스(0~40) + 직전 콤보 보너스(최대 100)`입니다.
- 정확도는 해당 모드의 `성공 시도 / 전체 시도`를 반올림한 실제 백분율입니다. 시도하지 않은 모드는 `—`로 표시합니다.
- 완료 시 최고 점수, 최고 콤보, 총 완료 횟수, 마지막 판의 모드별 시도·성공·오답·시간 초과를 저장합니다.
- 손상된 저장 데이터는 안전한 기본값으로 복구하고, 저장소 접근 실패 시 메모리 상태로 계속 플레이합니다.

## 입력·접근성·모바일

- 모든 핵심 버튼은 최소 44×44 CSS px 터치 영역을 유지합니다.
- 색상만으로 성공, 실패, 현재 모드나 규칙을 전달하지 않고 텍스트, 아이콘, `aria-label`, 라이브 알림을 함께 사용합니다.
- `P`는 일시정지/재개, 방향키는 궤도 입력과 별길 초점 이동에 사용합니다. 일시정지에서 재개하면 현재 문제만 새로 생성합니다.
- `prefers-reduced-motion: reduce`에서는 화면 전환, 캐릭터, 목표, 피드백 애니메이션을 끕니다.
- 1440×900과 390×844에서 가로 넘침 없이 홈, 플레이, 결과, 도움말을 사용할 수 있어야 합니다.

## 소리·자산 계약

- 홈에서는 `public/assets/velvet-tide.mp3`, 게임에서는 `public/assets/origami-pavements.mp3`를 반복 재생하고 화면 이동 시 이전 트랙을 정지한 뒤 처음 위치로 되돌립니다.
- 브라우저 자동재생이 실패하면 홈의 첫 포인터 입력에서 다시 시도합니다. 음악 토글은 배경음악과 Web Audio 효과음을 함께 끄고 켜며 로컬 설정을 사용합니다.
- 성공, 오류, 콤보와 입력 효과음은 사용자 입력 뒤 생성한 250ms 이하 Web Audio 오실레이터로 보완합니다.
- 기존 오리지널 PNG와 CSS 도형 외의 원격 자산은 사용하지 않습니다.
- 두 기존 MP3의 출처와 라이선스는 저장소에 기록되어 있지 않으므로 권리 확인 전까지 라이선스 안전 자산으로 단정하지 않습니다. 새 제3자 자산을 추가할 때는 파일과 라이선스 출처를 `THIRD_PARTY_NOTICES.md`에 기록합니다.
